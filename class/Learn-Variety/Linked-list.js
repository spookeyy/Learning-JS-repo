// Time Complexity: O(N)
// Auxiliary Space: O(N)

//Linked List
// node of a singly linked list
class Node {
    constructor(){ // constructor of a node
        this.data = 0 // data of a node 
        this.prev = null // previous node
        this.next = null // next node
    }
}

